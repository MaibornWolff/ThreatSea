apiVersion: v1
kind: Service
metadata:
  name: {{ include "threatsea.fullname" . }}
  labels:
    {{- include "threatsea.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "threatsea.selectorLabels" . | nindent 4 }}
