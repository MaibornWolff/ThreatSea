/**
 * @module catalog-measures.adapter - Adapter for
 *     the measures of the catalogue.
 */
import { createEntityAdapter } from "@reduxjs/toolkit";

export const catalogMeasuresAdapter = createEntityAdapter();
