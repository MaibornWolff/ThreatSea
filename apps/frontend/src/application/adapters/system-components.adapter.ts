/**
 * @module system-components.adapter - Defines the adapter
 *     for the system components.
 */
import { createEntityAdapter } from "@reduxjs/toolkit";

export const systemComponentsAdapter = createEntityAdapter();
