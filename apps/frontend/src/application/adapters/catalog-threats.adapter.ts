/**
 * @module catalog-threats.adapter - Defines the adapter
 *     for the catalogue threats.
 */
import { createEntityAdapter } from "@reduxjs/toolkit";

export const catalogThreatsAdapter = createEntityAdapter();
