/**
 * @module points-of-attack.adapter - Defines the adapter
 *     for the points of attack.
 */
import { createEntityAdapter } from "@reduxjs/toolkit";

export const pointsOfAttackAdapter = createEntityAdapter();
