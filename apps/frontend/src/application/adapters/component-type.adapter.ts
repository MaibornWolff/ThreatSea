/**
 * @module component-type.adapter - Defines the adapter
 *     for the component types.
 */
import { createEntityAdapter } from "@reduxjs/toolkit";

export const componentTypeAdapter = createEntityAdapter();
