{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run script: dev (backend)",
      "type": "node-terminal",
      "request": "launch",
      "command": "pnpm run dev",
      "cwd": "${workspaceFolder}/apps/backend"
    }
  ]
}
