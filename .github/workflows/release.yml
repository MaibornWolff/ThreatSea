name: Release

on:
  workflow_call:
    secrets:
      DOCKERHUB_USERNAME:
        required: true
      DOCKERHUB_TOKEN:
        required: true
      SO_API_TOKEN:
        required: true
  workflow_dispatch:

concurrency:
  group: release-${{ github.repository }}

jobs:
  compute-version:
    name: Compute Version
    runs-on: ubuntu-latest
    if: github.repository_owner == 'MaibornWolff' && github.ref_type == 'branch' && ((github.event_name == 'push' && github.ref_name == 'main') || (github.event_name == 'workflow_dispatch' && github.ref_name == 'next'))
    permissions:
      contents: write
    outputs:
      publish_new_release: ${{ steps.version.outputs.publish_new_release }}
      new_release_version: ${{ steps.version.outputs.new_release_version }}
    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Setup Repository
        uses: ./.github/actions/repository-setup

      - name: Compute next version (dry-run)
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        id: version
        run: |
          pnpm exec semantic-release --dry-run

  docker:
    name: Build and Push Docker Images
    runs-on: ubuntu-latest
    needs: compute-version
    if: needs.compute-version.outputs.publish_new_release == 'true'
    permissions:
      artifact-metadata: write
      attestations: write
      contents: read
      id-token: write # for Sigstore-based provenance signing
      packages: write
    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Login to Docker Hub
        if: github.ref_name == 'main'
        uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f # v4.6.0
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Login to GitHub Container Registry
        uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f # v4.6.0
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up QEMU
        uses: docker/setup-qemu-action@96fe6ef7f33517b61c61be40b68a1882f3264fb8 # v4.2.0

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@bb05f3f5519dd87d3ba754cc423b652a5edd6d2c # v4.2.0

      - name: Install cosign
        uses: sigstore/cosign-installer@6f9f17788090df1f26f669e9d70d6ae9567deba6 # v4.1.2

      - name: Extract metadata for next branch (prerelease)
        if: github.ref_name == 'next'
        id: meta-next
        uses: docker/metadata-action@dc802804100637a589fabce1cb79ff13a1411302 # v6.2.0
        with:
          images: |
            ghcr.io/maibornwolff/threatsea
          tags: |
            type=semver,pattern={{version}},value=${{ needs.compute-version.outputs.new_release_version }}
            type=ref,event=branch
          labels: |
            org.opencontainers.image.title=ThreatSea
            org.opencontainers.image.description=${{ vars.THREATSEA_DESCRIPTION }}
            org.opencontainers.image.vendor=MaibornWolff GmbH
            org.opencontainers.image.licenses=BSD 3-Clause

      - name: Build and push for next branch (prerelease)
        if: github.ref_name == 'next'
        id: build-next
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a # v7.3.0
        with:
          context: .
          file: ./Dockerfile
          platforms: linux/amd64,linux/arm64
          provenance: mode=max
          sbom: true
          push: true
          tags: ${{ steps.meta-next.outputs.tags }}
          labels: ${{ steps.meta-next.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Sign container image (next)
        if: github.ref_name == 'next'
        env:
          COSIGN_YES: "true"
        run: cosign sign -r ghcr.io/maibornwolff/threatsea@${{ steps.build-next.outputs.digest }}

      - name: Attest container image (next)
        if: github.ref_name == 'next'
        uses: actions/attest@1e69f48acb82d1966a394da916b4c1698aa569d6 # v4.2.2
        with:
          subject-name: ghcr.io/maibornwolff/threatsea
          subject-digest: ${{ steps.build-next.outputs.digest }}
          push-to-registry: true

      - name: Extract metadata for main branch
        if: github.ref_name == 'main'
        id: meta-main
        uses: docker/metadata-action@dc802804100637a589fabce1cb79ff13a1411302 # v6.2.0
        with:
          images: |
            maibornwolff/threatsea
            ghcr.io/maibornwolff/threatsea
          tags: |
            type=semver,pattern={{version}},value=${{ needs.compute-version.outputs.new_release_version }}
            type=semver,pattern={{major}}.{{minor}},value=${{ needs.compute-version.outputs.new_release_version }}
            type=semver,pattern={{major}},value=${{ needs.compute-version.outputs.new_release_version }}
            type=raw,value=latest
          labels: |
            org.opencontainers.image.title=ThreatSea
            org.opencontainers.image.description=${{ vars.THREATSEA_DESCRIPTION }}
            org.opencontainers.image.vendor=MaibornWolff GmbH
            org.opencontainers.image.licenses=BSD 3-Clause

      - name: Build and push for main branch
        if: github.ref_name == 'main'
        id: build-main
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a # v7.3.0
        with:
          context: .
          file: ./Dockerfile
          platforms: linux/amd64,linux/arm64
          provenance: mode=max
          sbom: true
          push: true
          tags: ${{ steps.meta-main.outputs.tags }}
          labels: ${{ steps.meta-main.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Sign container images (main)
        if: github.ref_name == 'main'
        env:
          COSIGN_YES: "true"
        run: |
          cosign sign -r index.docker.io/maibornwolff/threatsea@${{ steps.build-main.outputs.digest }}
          cosign sign -r ghcr.io/maibornwolff/threatsea@${{ steps.build-main.outputs.digest }}

      - name: Attest Docker Hub image (main)
        if: github.ref_name == 'main'
        uses: actions/attest@1e69f48acb82d1966a394da916b4c1698aa569d6 # v4.2.2
        with:
          subject-name: index.docker.io/maibornwolff/threatsea
          subject-digest: ${{ steps.build-main.outputs.digest }}
          push-to-registry: true

      - name: Attest GHCR image (main)
        if: github.ref_name == 'main'
        uses: actions/attest@1e69f48acb82d1966a394da916b4c1698aa569d6 # v4.2.2
        with:
          subject-name: ghcr.io/maibornwolff/threatsea
          subject-digest: ${{ steps.build-main.outputs.digest }}
          push-to-registry: true

  github-release:
    name: Create GitHub Release
    runs-on: ubuntu-latest
    needs: [compute-version, docker]
    if: needs.compute-version.outputs.publish_new_release == 'true'
    permissions:
      contents: write
      issues: write
    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Setup Repository
        uses: ./.github/actions/repository-setup

      - name: Create Release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          pnpm exec semantic-release

  create-sbom:
    name: Create SBOM
    needs: [compute-version, github-release]
    if: needs.compute-version.outputs.publish_new_release == 'true'
    permissions:
      contents: write
    uses: ./.github/workflows/create_sbom.yml
    with:
      version: ${{ needs.compute-version.outputs.new_release_version }}
      tag: v${{ needs.compute-version.outputs.new_release_version }}
    secrets:
      SO_API_TOKEN: ${{ secrets.SO_API_TOKEN }}
